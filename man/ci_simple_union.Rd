% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_naive_endpoint.R
\name{ci_simple_union}
\alias{ci_simple_union}
\title{Deprecated alias for the naive selected-endpoint benchmark}
\usage{
ci_simple_union(
  x,
  y,
  z,
  rxu_range = c(0, 0.8),
  grid_length = 80L,
  alpha = 0.05,
  Sigma_rho = NULL,
  cov_method = NULL,
  B_boot = 800L,
  seed = 123L,
  tol = 1e-10,
  make_psd = TRUE
)
}
\arguments{
\item{x}{Numeric vector, the residualised endogenous regressor.}

\item{y}{Numeric vector, the residualised outcome.}

\item{z}{Numeric vector, the residualised instrument.}

\item{rxu_range}{Numeric vector of length 2 giving the admissible domain D.}

\item{grid_length}{Deprecated; use \code{K}.}

\item{alpha}{Numeric significance level.}

\item{Sigma_rho}{Optional 3x3 sampling covariance of rho_hat on the estimator
scale. Supply the same object used by the MCUB routine so that the two
intervals rest on one bootstrap.}

\item{cov_method}{Deprecated and ignored; the bootstrap is always used.}

\item{B_boot}{Integer bootstrap replications used only when Sigma_rho is NULL.}

\item{seed}{Integer seed used only when Sigma_rho is NULL.}

\item{tol}{Numeric clamping tolerance.}

\item{make_psd}{Deprecated and ignored.}
}
\value{
See \code{\link{ci_naive_endpoint}}.
}
\description{
The former \code{ci_simple_union} applied a union-bound critical value
\code{qnorm(1 - alpha / (4 * K))}, which is not the construction in equation
(22). This alias forwards to \code{\link{ci_naive_endpoint}} and therefore
returns a different, narrower interval than the old function did.
}
